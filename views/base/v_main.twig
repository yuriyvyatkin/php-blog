<!DOCTYPE html>
<html lang="en">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="{{ BASE_URL }}assets/images/favicon.ico" rel="icon" type="image/x-icon" />
  <title>{{ title }}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="{{ BASE_URL }}assets/css/main.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <div class="wrapper">
    <header class="site-header pt-2 bg-success text-white">
      <div class="container">
        <div class="logo">
          <div class="logo__title h3">Blog</div>
        </div>
      </div>
    </header>

    <nav class="navbar navbar-expand-sm mb-3">
      <div class="container">
        <button class="navbar-toggler bg-primary navbar-dark" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <li class="nav-item {{ activeNav is same as('') ? 'highlight'}}">
              <a class="nav-link ps-0" href="{{ BASE_URL }}">Home</a>
            </li>
            <li class="nav-item {{ activeNav is same as('add_article') ? 'highlight'}}">
              <a class="nav-link" href="{{ BASE_URL }}add_article">Add article</a>
            </li>
            <li class="nav-item dropdown {{ activeNav is same as('categories_editor') ? 'highlight'}}">
              <a class="nav-link dropdown-toggle" href="javascript:void(0)" role="button" data-bs-toggle="dropdown">Categories editor</a>
              <ul class="dropdown-menu p-0">
                <li><a class="dropdown-item" href="{{ BASE_URL }}categories_editor/add_category">Add category</a></li>
                <li><a class="dropdown-item" href="{{ BASE_URL }}categories_editor/edit_category">Edit category</a></li>
                <li><a class="dropdown-item" href="{{ BASE_URL }}categories_editor/delete_category">Delete category</a></li>
              </ul>
            </li>
            <li class="nav-item {{ activeNav is same as('logs_journal') ? 'highlight'}}">
              <a class="nav-link" href="{{ BASE_URL }}logs_journal">Logs journal</a>
            </li>
            {% if userVerified %}
              <li class="nav-item">
                <a class="nav-link" href="{{ BASE_URL }}logout">Logout</a>
              </li>
            {% else %}
              <li class="nav-item {{ activeNav is same as('login') ? 'highlight'}}">
                <a class="nav-link" href="{{ BASE_URL }}login">Login</a>
              </li>
            {% endif %}
          </ul>
        </div>
      </div>
    </nav>

    <div class="site-content">
      <div class="container">
        {% block main %}
        <main>
          <h1>{{ h1 }}</h1>
          <hr>
          {{ content }}
        </main>
        {% endblock %}
      </div>
    </div>

    <footer class="site-footer pt-3 pb-3 mt-4 bg-success text-white">
      <div class="container">
        &copy; blog about everything
      </div>
    </footer>
  </div>
</body>
</html>
